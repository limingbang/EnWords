package com.jwstudio.enwords.exercise.view;

import android.support.v4.app.Fragment;

public interface IListeningTrainingView {

    // 显示不同模块的界面（听力训练，听写，词汇运用）
    void replaceFragment(Fragment fragment);

}
